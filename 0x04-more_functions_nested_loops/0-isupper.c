#include "main.h"
/**
* int _isupper(int c) checks for uppercase characters
* @c: char to check 
*
* return 1 if is uppercase and 0 othewise
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else 
return (0);
}
